# Bright Data CLI auth is handled by `bdata login` (stored in the CLI's own config) — no token needed here.

# AI toggle (Google Gemini, gemini-3.5-flash-lite) — advisory anomaly second-pass,
# AI-written heal diagnoses, and the weekly digest summary. Get a free-tier key at
# https://aistudio.google.com/apikey
# Leave blank / AI_ENABLED=false to run pure rule-based (default, zero external dependency).
AI_ENABLED=false
GEMINI_API_KEY=

FLANKWATCH_DB_PATH=
