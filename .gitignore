node_modules/
.env
api/db/*.db
api/db/*.db-journal
api/db/*.db-wal
api/db/*.db-shm
scratch/

# Per-category cron schedules and the AI on/off toggle — user runtime
# state, like the DB, not something to version-control. Created/written
# at runtime.
collectors/schedules.json
collectors/ai-state.json
dashboard/staged/
flankwatch-build-roadmap.md

# Demo-only static page for the live self-heal walkthrough — its own git
# repo (deployed separately to GitHub Pages), not part of this project.
demo-site/